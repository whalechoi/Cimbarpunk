# Cimbarpunk 1.0.1 Release Implementation Plan

**Goal:** Upgrade libcimbar to the latest upstream release and publish Cimbarpunk 1.0.1 with verified binaries, recursive corresponding source, and SHA-256 checksums.

**Architecture:** Keep the existing decoder integration; upstream v0.6.8 changes only web/index.html and web/main.js relative to the previous pin. Synchronize the application version and dependency provenance, then run the existing complete Windows verifier and package its output.

**Tech Stack:** C++20, Qt 6.8.4, CMake, Ninja, CTest, PowerShell, GitHub CLI.

**Spec:** User release request, AGENTS.md, README.md, existing desktop decoder design.

## Constraints

- Work directly on main. The current explicit release request authorizes the minimal release commit, push, tag, and publication.
- Preserve Qt, FFmpeg and vcpkg pins. Pin libcimbar v0.6.8 to bfb0c8e471820ae493cd3694ea6bed5d5ac06c37; preserve its nested samples pin.
- Do not reuse old binaries as version 1.0.1. The user waived Linux verification and authorized GitHub Actions for the fresh build because the previous host timed out. The workflow retains existing non-GUI build/deployment checks; Linux GUI and macOS remain unverified.
- Include full corresponding recursive source and license materials.

## Steps

- [x] Inspect clean main, upstream tag/HEAD, previous release process and upstream diff; verify no native code, build, nested dependency or license changes.
- [x] Update external/libcimbar, README.md, THIRD_PARTY_NOTICES.md and AGENTS.md to the exact new pin.
- [x] Set CMakeLists.txt, vcpkg.json, src/core/Version.h and tests/unit/tst_version.cpp from 1.0.0 to 1.0.1.
- [x] Run scripts/verify-windows.ps1 with the prescribed local Qt/vcpkg roots; record CTest count, package-local capture, license checks and bounded tray startup.
- [ ] Resolve Linux asset scope; create versioned Windows/source archives and checksums, inspect archive contents and verify archive hashes.
- [ ] Record fresh verification and release notes, inspect final diff and process hygiene.
- [ ] Commit only release changes on main, push without force, publish v1.0.1 against the exact commit, upload assets and read back release/tag/digests.

## Build evidence and corrections

Final build and artifact evidence: [2026-09-05 release verification](../../verification/2026-09-05-v1.0.1-release.md). Linux run 33938490249 succeeded with 16/16 non-GUI tests; earlier run failures below are retained as history.

- Windows command: `pwsh -NoProfile -File scripts/verify-windows.ps1 -QtRoot 'D:\Softwares\cqp-684\Qt' -VcpkgRoot 'D:\Projects\Cimbarpunk\.deps\vcpkg'`.
- Successful verifier exit: 0; CTest: 18/18, 0 failures, 7.95 seconds. Package-local FFmpeg real desktop capture, bounded tray startup, dependency and license checks passed. The tray process was force-stopped; no manual Exit-menu claim is made. Application signature status: NotSigned.
- Initial Windows dependency setup failed because the cached pip launcher exited 1 while `python -m pip` worked. Reinstalling the same pip 26.2.1 regenerated the launcher; no dependency pins were changed. The complete verifier was rerun afterward.
- Linux run 33930953011 failed during Qt configuration because the runner's Clang exports referenced missing libclangBasic.a. The workflow now installs libclang-18-dev and llvm-18-dev. Replacement run: https://github.com/whalechoi/Cimbarpunk/actions/runs/33931151888 . Its final result belongs in the published release notes.
- Independent review identified a directory prefix in the artifact checksum file; generation now runs inside packages. The workflow derives application version from CMakeLists.txt with numeric semantic-version validation, tested with current/future versions and invalid inputs; it is not pinned to 1.0.1.
- Publishing required a candidate commit before running Actions. Later workflow/documentation-only commits do not change application build inputs; compare the Linux artifact's linux-source-commit.txt against the final tag and preserve this provenance in release notes.
